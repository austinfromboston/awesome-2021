export default function Intro({ headline, event_date, facebook_link, location, updates }) {
  return (
    <React.Fragment>
      <h2>{headline}</h2>
      <h4>{event_date}</h4>
      <h4>{location}</h4>
      <p>
        {(facebook_link && 
      <a
        sx={{ variant: `buttons.toggle`, fontWeight: `semibold`, display: `block`, mx: `auto`, mb: 3 }}
        href={facebook_link}
        target="_blank"
      >
        Awesome on Facebook
      </a>
        )}
      </p>
      <p dangerouslySetInnerHTML={{__html: updates}}></p>
      <p>
        <a href="/faq">Printable FAQ</a>
      </p>
      <p>
        <a href="/bring">What to Bring</a>
      </p>
    </React.Fragment>
  );
}
